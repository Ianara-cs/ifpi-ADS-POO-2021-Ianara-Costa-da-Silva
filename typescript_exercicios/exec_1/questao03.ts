// Letra A
enum siglas {PI, CE, MA}
for(let i = 0; i <= siglas.MA; i++){
    console.log(siglas[i])
}

// Letra B
//enum siglas {PI, CE, MA}
for(let i = 0; i <= siglas.MA; i ++){
    console.log(i)
}
/* Primeiro foi criado um enum nomeado de "siglas" depois
Usando a estrutura de repetição foi Executado um trecho do código por uma quantidade
específica de vezes, ou seja, até a quantidade de index que o enum siglas tem
e na saída foi imprimido um index desse enum, do início ao fim, a cada volta da estrutura de repetição.
*/
